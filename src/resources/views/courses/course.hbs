<div class="mt-4">
    <div class="row">
        <div class="col-md-3">
            <h5>Learn {{ course.name }}</h5>
        </div>
        <div class="col-md-9">
            <div class="course-item">
                <h2>{{ course.name }}</h2>
                <img src="{{ course.image }}" alt="{{ course.name }}">
                <p>{{ course.description }}</p>
            </div>
        </div>
    </div>
</div>
